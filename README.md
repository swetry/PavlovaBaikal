# Безопасный SQL Runner
## Отборочное задание для ИТ-школы КОД БАЙКАЛА 2026.

**Описание** <br>
Простой Python-скрипт для безопасного выполнения SQL-запросов к PostgreSQL. Если в запросе отсутствует ограничение на количество строк, скрипт автоматически добавляет LIMIT 5. Любые попытки изменения данных (DELETE, UPDATE, INSERT и др.) блокируются до выполнения.

**Стек технологий**<br>
Python 3.10+<br>
psycopg2-binary — драйвер для PostgreSQL<br>
python-dotenv — работа с переменными окружения<br>

**Требования**<br>
Установленный Python версии 3.10 или выше<br>
Доступ к базе данных PostgreSQL<br>

**Установка**<br>
Клонируйте репозиторий:<br>
git clone <ссылка_на_ваш_репозиторий><br>
cd safe-sql-runner<br>
(Замените <ссылка_на_ваш_репозиторий> на URL вашего проекта)<br>
Создайте виртуальное окружение (рекомендуется):<br>
python -m venv venv<br>
source venv/bin/activate  # Linux/macOS<br>
venv\Scripts\activate     # Windows<br>
Установите зависимости:<br>
pip install -r requirements.txt<br>
Настройте переменные окружения:<br>
cp .env.example .env<br>

**Отредактируйте файл .env под свои данные** <br>
Пример корректного .env:<br>
env<br>
DB_HOST=localhost<br>
DB_PORT=5432<br>
DB_NAME=test_db<br>
DB_USER=postgres<br>
DB_PASSWORD=<br>
**Запуск**<br>
python main.py<br>
Программа установит соединение с базой данных и перейдет в режим ожидания ввода запросов.<br>

**Примеры использования**<br>
Простой запрос:<br>
sql<br>
SELECT * FROM users;<br>
Выполняется запрос:<br>
SELECT * FROM users LIMIT 5;<br>
Результат в консоли:<br>

| id   | name         | age | group_name       |<br>
| 1    | Иванов И.И.  | 20  | ПМИ-21           |<br>
| 2    | Петров П.П.  | 19  | ПМИ-21           |<br>
| 3    | Сидорова А.А.| 21  | КБ-20            |<br>
| 4    | Смирнов С.С. | 20  | КБ-20            |<br>
| 5    | Кузнецов К.К.| 22  | ИБ-19            |<br>
Всего строк: 5<br>
Запрос с LIMIT:<br>
sql<br>
SELECT name FROM products LIMIT 10;<br>
LIMIT будет использован как указан пользователем.<br>
Запрещенный запрос:<br>
sql<br>
DELETE FROM users;<br>
Скрипт заблокирует выполнение с сообщением об ошибке.<br>

**Выход из программы**<br>
Для завершения работы введите:<br>
exit<br>
quit<br>
q<br>

**Примечания по безопасности**<br>
Не храните пароль в .env файле<br>
Используйте интерактивный ввод пароля при запуске<br>
Все запросы проверяются на безопасность<br>

_Подготовила Павлова Мария_
